# Copy to .env and adjust. Everything has a working default for the local stack.
FP_KAFKA_BOOTSTRAP=localhost:19092
FP_KAFKA_TOPIC=transactions
FP_KAFKA_GROUP_LANDING=fp-landing
FP_KAFKA_GROUP_FEATURES=fp-features

FP_REDIS_HOST=localhost
FP_REDIS_PORT=6379

FP_MLFLOW_TRACKING_URI=http://localhost:5001
FP_MLFLOW_EXPERIMENT=fraudpulse

# Optional: drift alerts
FP_SLACK_WEBHOOK_URL=
